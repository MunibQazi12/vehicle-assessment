/**
 * Form Components
 * Export all form-related components
 */

export { FormContent } from "./FormContent";
export { FormModalSRP } from "./FormModalSRP";
export { FormInlineVDP } from "./FormInlineVDP";

// Standalone form components for pages
export { FormSSR } from "./FormSSR"; // Server-side fetching (for server components)
export { FormClient } from "./FormClient"; // Internal - handles form interactivity
